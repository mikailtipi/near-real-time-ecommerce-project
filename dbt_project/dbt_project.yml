name: ecommerce_pipeline
version: "1.0.0"
config-version: 2

profile: ecommerce_pipeline

model-paths: ["models"]
test-paths: ["tests"]

models:
  ecommerce_pipeline:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
